# Phase 3 — 사업 통합 검토 (진행 중)

> **전제:** Phase 2 완료 (일제점검 47항목, N/A 엔진, 프로필 UI, 결과 화면)  
> **마지막 작업:** 2026-07-04 — 실데이터 E2E, X07 검증 완료. **나머지 항목 정교화는 다음 세션.**

## 목표

로드맵 3단계: **사업 1건 + ①~④ 자료 + 일제점검 체크리스트 1회** 검토

## 구현 현황

| # | 작업 | 상태 |
|---|------|------|
| 3-1 | 검토 화면 ①~④ 다중 업로드 (일제점검 선택 시) | ✅ |
| 3-2 | `run_case_review_pipeline` — OCR·파싱 병합 후 1회 비교 | ✅ |
| 3-3 | 제출 자료 유무 = 실제 업로드 파일 기준 (N/A 연동) | ✅ |
| 3-4 | 교차 항목 규칙 (`cross_rules.py` — JC-01, X07) | ✅ 실데이터 확인 |
| 3-5 | 나머지 47항목 실데이터 정교화 | 🔲 **다음 세션** |
| 3-6 | 실데이터 골든케이스 교체 | 🔲 팀 정리 후 |

## 사용

1. 자료 검토 — 체크리스트 **일제점검(통합)** (기본 선택, checklist_id=9)
2. ①~④ 슬롯에 **있는 자료만** 업로드
3. STEP 2에서 집행 세목 선택 후 검토
4. 결과: N/A 건수·프로필 요약·항목별 검토내용(발췌)

유형별 단일 자료 검토는 체크리스트를 유형별로 바꾸면 기존과 동일.

서버 재시작: `SAFE_restart.bat` (에이전트는 코드 수정 후 재시작 수행)

## 확정된 도메인·구현 메모 (다음 세션용)

### 검토 단위
- **사업 1건 + ①~④ 묶음** + 일제점검 48항목(47+X07)
- 지식DB/Gemini로 일제점검 생성 ❌ → `scripts/seed_inspection_checklist.py` 만 사용

### ② 집행내역서 (항상 엑셀, 26열)
| 열 | 필드 | 용도 |
|----|------|------|
| **L** | 집행거래일자 | X07 집행일 |
| **P** | 보조세목(통계목) | 세목 (시설비 등) |
| **T** | 지방비 집행금액 | 금액 |
| **U** | 자부담 집행금액 | 금액 |
| 집행금액 | **T+U** | 행별 합계 |

파서: `parser.parse_execution_detail_excel` (openpyxl, read_only 미사용)

### 교차 규칙 (`cross_rules.py`)
| 항목ID | 규칙 | 비고 |
|--------|------|------|
| **X07** | ① 사업기간 vs ② L열 집행거래일자 | ✅ 실데이터 검증 (기간 외 → F) |
| **JC-01** | ① 예산집행(사용)계획 세목별 금액 vs ② 집행액 | ①②만 필요 (**③ 무관**). 추가 정교화는 다음 세션 |

### 결과 화면
- N/A(`A`) 건수·적용/전체
- 프로필 요약 (`remark`에 `PROFILE|...` 저장)
- 검토내용: N/A 사유 / 교차 수치 / 유사도 시 문서 발췌(`「…」`)

### 주요 파일
- `na_engine.py` — N/A·교차 W
- `cross_rules.py` — JC-01, X07
- `parser.py` — 사업기간, 예산집행계획, 집행내역 엑셀
- `checker.py` — 비교·발췌·판정 문구
- `routers/review_router.py` — 사업 통합 파이프라인
- `static/js/review.js`, `templates/review.html` — 다중 업로드 UI

```powershell
python test_cross_rules.py
python test_inspection_na.py
```

## 다음 세션 (우선)

1. **나머지 일제점검 항목 실데이터 정교화** (유사도만이 아닌 규칙·발췌 품질)
2. JC-01 예산집행계획 세목↔집행 세목(P열) 매칭 고도화
3. JC-02 등 추가 교차 규칙
4. 실데이터 기반 G* 골든케이스 교체
